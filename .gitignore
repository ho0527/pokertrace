db.sqlite3
node_modules/
__pycache__/
*.pyc
*.log
# 資料庫完整備份含 user/token 等 PII，不進版控（schema-only 的 *_schema.sql 可留）
# 目錄名以 ! 開頭，gitignore 會誤判為否定符號，需用 \ 轉義
\!SQL/*.dump
backend/backup/dumps/

# 備份檔只在測試機產生,正式機是純部署目標,不應出現也不進版控
*_old_t*

# Word 暫存鎖定檔
~$*

# 正式機不做建置(nginx 直接吃 tailwind.css 產物), 不需要 npm 檔
package.json
package-lock.json
node_modules/
